# vscode
.vscode 

# Intellij
*.iml
.idea

# npm
node_modules

# Compiled main.js is tracked and committed along with source for this repo,
# and is also published via GitHub Releases for Obsidian community installs.
# Node ignores it while running local builds but we keep tracking it.
# main.js

# Exclude sourcemaps
*.map

# obsidian
data.json

# local workspace memory (agent tooling)
.workbuddy/
.workbuddy-ai/

# 构建护栏失败时的临时备份（正常情况下构建结束即删除）
main.js.guardbak

# Exclude macOS Finder (System Explorer) View States
.DS_Store
nul
