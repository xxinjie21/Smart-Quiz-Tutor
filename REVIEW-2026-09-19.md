# Smart Question Tutor (智学助手) v2.3.0 — 代码审查报告

- 审查日期：2026-09-19
- 审查范围：`src/` 全部 53 个 TS 文件、`tests/`、构建脚本、`styles.css`、`manifest.json`、README
- 方式：第一、二轮为静态阅读 + 只读检查（未修改文件）；**第三轮为「全部修复 + 回归测试 + 复审」，见第 7 节**
- 检查命令：`npx tsc -noEmit -skipLibCheck` / `npx eslint .` / `npx vitest run`

> 📌 **第 1~6 节是修复前的原始问题清单，保留其原始描述与指标。**
> **修复后的状态、验证证据与复审结论见第 7 节。**

---

## 0. 自动化检查结果

| 检查 | 结果 |
|------|------|
| TypeScript (`tsc --noEmit`) | **0 错误** |
| ESLint (`eslint .`) | **0 error / 12 warning** |
| 单元测试 (`vitest run`) | **239 / 239 通过**（19 个文件，4.85s） |
| i18n 字典完整性 | **532 键 zh/en 完全对齐**，源码 451 个 `t()`/`tf()` 字面量 key **无缺失** |
| 构建产物 | `main.js` 2.75 MB，与当前源码**同步**（已验证新字符串均已入包） |

**总体结论**：代码质量中上，没有发现崩溃级或必现的数据损坏缺陷。但存在 **1 个真实的数据丢失风险**、**3 个功能性 bug**、以及一批合规与可维护性隐患。

> ⚠️ **第二轮深挖（见第 6 节）发现了一个比第一轮所有问题都严重的高影响 bug**：
> **AI 输出格式与导出解析器不兼容，导致 Word / PDF / 无答案版 / 仅答案版四个导出功能同时出错。**
> 建议优先阅读 6.1。

---

## 1. 🔴 高优先级

### 1.1 绝对路径分支绕过回收站，永久删除文件

**位置**
- `src/services/knowledgeService.ts:238`（`removeStaleTagFiles` 的 abs 分支）
- `src/services/vaultDataService.ts:177`（`deleteWrongNote` 的 abs 分支）

> 注：第二轮深挖发现这不是 2 处疏漏，而是**共 8 处的全局设计问题**，详见 **6.3**。

**问题**：同一段逻辑里，vault 内路径走 `fileManager.trashFile()`（进回收站可恢复），绝对路径分支却走 `fs.unlinkSync()`（**永久删除，不可恢复**）。

```ts
// knowledgeService.ts
if (isAbs(folder)) {
    for (const f of listMdFiles(folder)) {
        if (keep.has(f.replace(/\.md$/, ""))) continue;
        if (isKnowledgeIndexContent(content)) fs.unlinkSync(joinPath(folder, f)); // ← 永久删除
    }
    return;
}
// vault 分支
if (isKnowledgeIndexContent(content)) await this.p.app.fileManager.trashFile(child); // ← 回收站
```

**后果**：当用户把「根文件夹」配置成 vault 之外的绝对路径时，**重建知识点索引会永久删除过期索引笔记**，且无任何撤销途径。

**附带的误删风险**：`isKnowledgeIndexContent()` 的判据是 `/^---\n\s*tags:\s*\[知识点\]\s*\n/` —— 用户手写的笔记只要 frontmatter 第一行恰好是 `tags: [知识点]`，就会被判定为「插件生成的索引」而在重建时删除。这与 README 中「知识点文件夹中的手写笔记永远不会被误删」的承诺并不完全成立。

**建议**：abs 分支统一改为移入系统回收站（或先改名到 `.trash` 子目录）；索引判据加一个插件自有的标记字段（如 `qg-index: true`）来避免误判。

---

### 1.2 `openConfirm` / `openInput` 按 Esc 后 Promise 永不 resolve

**位置**：`src/views/ui/modals.ts`

```ts
onClose() {
    this.contentEl.empty();
    this.resolve = null;   // ← Esc 关闭时 resolve 被清空，Promise 悬空
}
```

**问题**：Obsidian 的 `Modal` 默认会拦截 Esc 并调用 `close()`，这条路径不经过「确定 / 取消」按钮，因此 `this.resolve` 已被置为 `null`，返回给调用方的 Promise **永远不会 resolve**。

**影响面**：所有 `await openConfirm(...)` / `await openInput(...)` 之后的代码静默不执行，且每次泄漏一个 pending Promise。

- `chatPanel.ts` — `deleteSelectedSessions` / `deleteSessionById` / `renameSessionById` / `clearSession`

**建议**：在 `onClose()` 中先以默认值 resolve（`ConfirmModal` → `false`，`InputModal` → `null`），再清空引用。

---

## 2. 🟠 功能性 bug

### 2.1 根文件夹为绝对路径时，AI 对话「仅插件知识库」检索为空

**位置**：`src/views/chatPanel.ts:653-677`（`collectCandidates`）

```ts
for (const d of [questionFolder, wrongBookFolder, noteViewFolder, knowledgeFolder]) {
    const p = this.plugin.rootPath(d);      // 绝对路径时返回 "D:/.../题目"
    if (p) pluginDirs.push(p);
}
base.push(...all.filter(f => pluginDirs.some(p => f.path.startsWith(p + "/")))); // ← f.path 是 vault 相对路径，永不匹配
```

`rootPath()` 在 `rootFolder` 为绝对路径时返回文件系统绝对路径，而 `f.path` 是 vault 相对路径，`startsWith` 永远为 false → **候选集为空，AI 完全不参考笔记**。

**附带**：`getScopeFiles(base, scope, pluginDirs)` 是对已经过滤过的 `base` 的二次过滤，属冗余。

### 2.2 `isCasualQuery` 把 ≤4 字符的问题一律当闲聊

**位置**：`src/services/chatService.ts:31-36`

```ts
if (t.length <= 4) return true;   // 「勾股定理」「牛顿定律」「SM-2」→ 跳过检索
```

短而明确的问题会被判定为闲聊，直接跳过笔记检索，AI 凭模型记忆作答。与「回答基于你的笔记」的产品承诺冲突。

**建议**：长度阈值降到 2，或改为「长度 ≤4 **且** 命中寒暄正则」才判定为闲聊。

### 2.3 原生设置页「每敲一个字符写一次磁盘」

**位置**：`src/views/settingTab.ts:55,69`

`Setting.addText(...).onChange()` 在 Obsidian 中每次 input 事件都会触发 → 每次都调用 `saveSettings()` 全量写 `data.json`。输入 `http://127.0.0.1:11434` 会触发 22 次磁盘写入。

**同时存在两套不一致的写入时机**：侧边栏设置页（`src/views/sidebar/settings.ts:29,86`）用的是 `change` 事件（失焦才写）。「单一来源」只统一了 schema，没有统一写入语义。

仓库里已有 `src/utils/debounce.ts`，此处未使用。

### 2.4 schema 里的 `min` / `max` 未被强制

`parseSettingValue()`（`settingsSchema.ts:132`）只做 `parseFloat/parseInt || 0`，不做范围钳制；`min`/`max` 仅作为 HTML 属性出现。结果：`temperature` 可以存 999，`countSingle` 可以存 -5。只有 `easePreset` 走了 `clampEase()`。

### 2.5 `EASE_PRESETS` 标签与语义自相矛盾

**位置**：`src/constants.ts:54-58`

```ts
{ label: "慢速", factor: 2.7, hint: "间隔增长快、复习次数少，…" },
{ label: "快速", factor: 2.3, hint: "间隔增长慢、复习更频繁，…" },
```

label 说「慢速」，hint 说「间隔增长**快**」—— 两者方向相反。用户极易选反。建议改为「少复习 / 标准 / 多复习」这类不含歧义的措辞。

### 2.6 `MarkdownRenderer` 复用同一个 Component（内存累积）

**位置**：`src/views/chatPanel.ts:514`

`appendBubble()` 每次都用同一个 `this.component` 调用 `MarkdownRenderer.render(...)`。该 API 会在传入的 Component 上挂载渲染子组件；同一 Component 被反复复用，子组件会累积到视图卸载为止。长时间使用（大量消息 + 反复切换会话）会持续增长内存。建议每次渲染传入独立的 `MarkdownRenderChild`。

---

## 3. 🟡 合规 / 可维护性

### 3.1 `main.js` 2.75 MB，且靠正则「手术」过审

**体积问题**：`await import("mammoth" / "pdf-parse" / "pdfjs-dist")` 由于 esbuild 配置为 `format: "cjs"` + 单文件 `outfile`，无法做代码分割，三者（含 JSZip）**全部被内联进 main.js**。懒加载只延迟了*执行*，没有减少*体积*与*启动解析成本*——Obsidian 每次启动都要解析这 2.75 MB，即使该用户从不打开 PDF。社区插件的常见量级是几百 KB。

**构建脚本脆弱性**：`esbuild.config.mjs:69-100` 包含 4 段针对**压缩后产物**的正则替换（剥离 `createElement("script")`、把 `%eval%` 替换为 `void 0`），依赖 minifier 变量名与代码形状。`String.replace` 在匹配失败时**静默不报错**，依赖升级后可能悄悄失效。

值得肯定的是有 `tests/buildGuard.test.ts` 做兜底断言（no eval / no script injection），这是好实践。但更稳妥的做法是用 esbuild 插件在**源码层**替换这些包，而不是在产物上打补丁。

### 3.2 12 条 ESLint warning（0 error）

| 位置 | 规则 | 说明 |
|------|------|------|
| `views/settingTab.ts:9` | `prefer-setting-definitions` | **未实现 `getSettingDefinitions()`** → 在 Obsidian **1.13+** 上，本插件设置项**不会出现在设置搜索中**。`minAppVersion` 为 1.12.0，但多数用户已在 1.13+。这条最值得优先修。 |
| `services/knowledgeService.ts:135,193`、`views/sidebar/notes.ts:234`、`views/sidebar/questions.ts:28` | `no-tfile-tfolder-cast` | 4 处 `as unknown as TFile` 伪造对象（绝对路径分支），喂给需要真 TFile 的代码有运行时风险 |
| `services/documentService.ts:371` | `no-global-this` | 应使用 `window` / `activeWindow` 以兼容 popout 窗口 |
| `views/sidebar/wrong.ts:82,165,229`、`views/sidebar/notes.ts:123`、`views/sidebar/questions.ts:181`、`views/sidebarView.ts:350` | `no-alert` | 6 处原生 `confirm()`。项目里已经写了 `openConfirm` 自定义弹窗并在 chatPanel 用上了，两套并存；原生弹窗还会阻塞渲染进程 |

### 3.3 `withPdfEnvironment` 直接改宿主进程全局对象，且无并发保护

**位置**：`src/services/documentService.ts:368-395`

转换 PDF 时向 `globalThis` 挂载/删除 `DOMMatrix`、`ImageData`、`Path2D`、`pdfjsWorker`、`navigator`，完成后还原。

**风险**：没有互斥。批量识别多份试卷时，若两个 PDF 转换重叠，内层的 `finally` 会在外层仍在使用这些全局时把它们删掉。建议改为串行队列（或把 polyfill 限制在 worker/局部作用域内）。

### 3.4 生命周期与清理

- `views/sidebar/home.ts:188-190` 用 3 个 `setTimeout`（200/600/1200ms）反复重试滚动定位，**未注册也未清理**。这种「多档延时重试」本身就是脆弱信号。
- 全项目 **174 处 `addEventListener` vs 仅 3 处 `register*`**。AGENTS.md 明确要求用 `register*` 助手注册以便安全卸载。挂在 view 自有 DOM 上的监听随元素销毁尚可接受，但 window 级监听与定时器会泄漏。
- `main.ts:236` 用 `this.registerInterval()` 注册 `setTimeout` —— 语义错位（浏览器中 clearInterval/clearTimeout 共用 id 空间，因此碰巧能工作）。

### 3.5 「停止」不会真正中断请求

`chatPanel.requestChat`（`chatPanel.ts:746-768`）与 `sidebarView.callAIWithPrompt`（`sidebarView.ts:586-609`）都只是用 `Promise.race` 放弃等待，底层 `requestUrl` 仍在后台跑完。代码注释已承认这点（"取消/超时后丢弃迟到的错误"）。功能上可接受，但 UI 上「停止」的语义需要明确。

### 3.6 文件规模超标

| 文件 | 行数 | AGENTS.md 建议 |
|------|------|----------------|
| `src/views/chatPanel.ts` | 773 | 200–300 |
| `src/views/sidebarView.ts` | 745 | 200–300 |

`chatPanel.ts` 把 UI 构建、会话管理、引用管理、检索、发送全部塞在一个类里。v2.2.0 曾把 `sidebarView.ts` 从 3700 行拆成 13 个模块，但 chatPanel 是新增的单体。

### 3.7 344 处内联 style

**分文件**：`answer.ts` 48、`generate.ts` 40、`knowledge.ts` 38、`home.ts` 38、`noteGen.ts` 33、`wrong.ts` 20、`exam.ts` 20…

**好消息**：绝大多数用的是 Obsidian 语义变量（`var(--text-muted)` ×72、`var(--background-modifier-border)` ×51、`var(--interactive-accent)` ×41…），主题适配基本没问题。`styles.css` 里的 hex 值基本都是 `var(--color-red, #e5534b)` 这类**回退值**，并且有 `.theme-dark` / `.theme-light` 分区，CSS 这一层做得不错。

**坏消息**：内联样式绕过 `styles.css`，用户的 CSS 片段与主题无法覆盖。上一次提交 `feat(ui): unify hardcoded colors via semantic tokens` 实际只落到了 CSS 层，组件层（344 处）没有跟进。

### 3.8 文档与仓库状态不同步

- README v2.3.0 条目写「227 tests pass」，实际为 **239**。
- 整个 v2.3.0 改动（**43 个修改 + 12 个新增文件**，含 `sm2.ts`、`date.ts`、`chatStorage.ts`、`settingsSchema.ts`、`views/ui/`、`views/sidebar/shared/` 等新模块）**全部处于未提交状态**，但 `manifest.json` 已 bump 到 2.3.0、README 已按「已发布」口吻撰写。建议先提交，再按流程打 release（tag 必须与 manifest 版本严格一致，不带 `v` 前缀）。

---

## 4. ✅ 做得好的地方

- **工程纪律**：tsc 0 错误、239/239 测试通过、ESLint 0 error。测试覆盖到 SM-2、日期、聊天存储、知识索引、文档解析、i18n 等关键模块。
- **i18n 质量高**：532 键 zh/en **完全对齐**，无孤键、无缺失（已逐个比对源码中 451 个 `t()`/`tf()` 字面量）。`t()` 带回退，界面永不空白。
- **SM-2 实现纯正**：EF 调整发生在间隔计算**之后**（符合原始算法顺序）、`q < 3` 时 `repetitions` 归零且 `interval = 1`、EF 下限 1.3、`Math.round(EF*100)/100` 防浮点漂移。模块难度因子作为**新卡初始 EF** 注入，与 README 描述一致。
- **纯函数设计**：`sm2.ts` / `date.ts` / `review.ts` / `chatService.ts` 无 IO、无副作用，易测。
- **本地时区日期工具**（`date.ts`）正确规避了 `toISOString()` 的 UTC 偏移坑，这是很多同类插件踩过的坑。
- **`ChatAdapter` 接口 + 内存降级模式**（`chatStorage.ts` + `chatPanel.ts` 的 `memoryMode`）边界处理得不错：存储不可用时降级为内存会话并给出明确提示。
- **构建产物护栏测试**（`buildGuard.test.ts`）断言 `main.js` 无 `eval`、无 `createElement("script")` —— 把「过审」这件事变成了可回归的测试。
- **检索算法考虑周全**：CJK 2/3-gram 分词、文件名/路径加权、两段式候选排序（只读取命中文件）、注水法公平分配引用预算、在自然边界截断。

---

## 5. 建议的修复顺序（已并入第 6 节结论）

见第 6.6 节。

---

## 6. 第二轮深挖（核心业务链路）—— 新增发现

第一轮偏重架构、生命周期与构建；第二轮逐个读了出题 / 解析 / 导出 / 复习评分的实现，并**用真实函数跑了探针验证**（探针放在仓库外，未改动项目代码）。

### 6.1 🔴🔴 AI 输出格式与导出解析器不兼容（一次性影响 4 个导出功能）

**根因**：提示词**强制要求加粗题号**，但三个下游解析器**只认裸题号**，且全项目没有任何一处剥离 `**`。

证据链（三处互相矛盾）：

1. `services/questionService.ts:36` —— `QUESTION_FORMAT_RULES` 铁律 #2：
   「题号格式固定为：**数字.** 题干文本（注意加粗）」
   `buildExamExtractPrompt` 的格式示例同样写 `**1.** 题干文本`
2. `services/questionService.ts:361` —— `validateGenerated()`：
   ```ts
   const questionCount = (text.match(/\*\*\d+\.\*\*/g) || []).length;  // 只认加粗形式
   return { ok: sectionCount > 0 && questionCount >= 1 && hasAnswer, ... };
   ```
   `generate.ts:244` 据此判断「格式不达标」并触发重试 —— 也就是说**插件主动把 AI 逼进一个它自己的导出器读不懂的格式**。
3. 全项目**没有任何一处剥离 `**`**（`grep '**' src | grep replace` 零结果）。

而下游三个消费方只认 `^\d+[.、]` 裸格式：

| 消费方 | 位置 | 实测结果 |
|---|---|---|
| `parseExamBlocks` | `utils/exporter.ts:36` | `**1.**` 被判为 `text` 而非 `question` → Word/PDF 里**显示字面量 `**1.**`**、题号不加粗、题目间距与「题→答案」分组逻辑全部失效 |
| `stripAnswersForExport` | `utils/text.ts:51` | 「无答案版」：`skip` 置位后无法被 `**2.**` 复位 → **该题型下第 2 题起的题干与选项全部丢失** |
| `extractAnswersForExport` | `utils/text.ts:67` | 「仅答案版」：`current` 永远取不到题号 → **所有答案行都没有题号**，无法与题目对应 |

**实测输出**（直接调用真实函数）：

```
输入：## 单选题 / **1.** 题干一 / A.甲 / B.乙 / 答案：A / 解析：因为甲 / **2.** 题干二 / A.丙 / 答案：B

parseExamBlocks →  heading "单选题"
                    text    "**1.** 以下哪个是正确的？"   ← 分类为 text，不是 question
                    option  "A. 甲" / "B. 乙"
                    answer  "答案： A"
                    explanation "解析："

导出 HTML        → 包含字面量 '**1.**' = true ；包含 '<strong>1.' = false

无答案版导出     → ## 单选题 / **1.** 题干一 / A.甲 / B.乙 / ## 判断题 / **3.** 题干三 / A.正确 / B.错误
                   ★ 第 2 题整题（题干+选项）消失，只剩题号 1. 和 3.

仅答案版导出     → 答案：A / 解析：因为甲 / 答案：B / 答案：A
                   ★ 全部答案行都没有题号
```

**为什么作者可能没发现**：MD 导出走的是原样输出（`**1.**` 在 Markdown 里本来就该渲染成加粗），完全正常。只有 Word / PDF / 无答案版 / 仅答案版这四条链路会暴露。

**修复方向**（推荐第一个）：
- 在 `parseExamBlocks`、`stripAnswersForExport`、`extractAnswersForExport` 的题号正则前统一加上可选的 `(?:\*\*)?` 前缀；
- 或在导出前统一剥离 `**`（但会连带影响 MD 导出，不推荐）。

### 6.2 🔴 批量删除 N 条错题 → 触发 N 次全量知识点索引重建

`views/sidebar/wrong.ts:84` 循环调用 `plugin.deleteWrongNote(p)`：

```ts
for (const p of selected) { try { await view.plugin.deleteWrongNote(p); } catch { /* skip */ } }
```

而 `services/vaultDataService.ts:183` 在每次删除后都会：

```ts
this.invalidateCache();
await this.p.rebuildKnowledgeIndex();   // 扫描 题目/错题/笔记/知识点 四个目录，重写全部索引，再做健康扫描
```

**删除 20 条错题 = 20 次全量索引重建**。这是插件里最重的操作之一，批量删除会明显卡顿。

建议：把 `rebuildKnowledgeIndex()` 提到循环外调用一次，或给 `deleteWrongNote` 加一个 `skipRebuild` 参数。

### 6.3 🔴 绝对路径下的永久删除是系统性问题（共 8 处，非上一轮所述的 2 处）

上一轮只定位到 2 处，实际 grep 出 **8 个删除点**，每一个都带 `isAbs` 分支走 `fs.unlinkSync`（永久删除，不进回收站）：

| 位置 | 操作 |
|---|---|
| `services/knowledgeService.ts:238` | 清理过期知识点索引 |
| `services/vaultDataService.ts:177` | 删除错题 |
| `utils/fs-utils.ts:87`（`deleteFileAbs`） | 通用删除工具 |
| `views/sidebar/knowledge.ts:100` | 删除索引笔记（经 `deleteFileAbs`） |
| `views/sidebar/notes.ts:125` | 删除笔记 |
| `views/sidebar/questions.ts:183` | 删除题目文件 |
| `views/sidebar/wrong.ts:230` | 删除错题（经 `deleteFileAbs`） |
| `views/sidebarView.ts:354` | **批量删除选中文件** |

**结论**：只要「根文件夹」配成绝对路径，插件的**所有删除操作都绕过回收站且不可恢复**，包括批量删除；而 vault 内路径统一走 `fileManager.trashFile`。这个不对称是全局设计问题，不是个别疏漏。

### 6.4 🟠 frontmatter 解析/重写会在每次复习评分时破坏用户自定义 YAML

`views/sidebarView.ts:382-419`（`updateReviewSchedule`）在每次评分后用 `buildFM(parseFM(content)) + body` **重写整个 frontmatter**。而 `parseFM` / `buildFM` 只支持扁平标量。

**实测往返结果**：

| 原始 frontmatter | 重写后 | 结果 |
|---|---|---|
| `note: \|` + 两行缩进内容 | `note: "\|"` | **两行备注内容全部丢失** |
| `note: "第一行⏎第二行"` | `note: "第一行"` | **只保留第一行** |
| `meta:` + 缩进子键 | `meta: "…垃圾字符串…"` + 伪造键 `b: "2"` | **结构被破坏，且多出伪造键** |
| `note: "普通备注"`（单行） | 与原始一致 | ✅ 正常 |

**触发场景**：复习评分（插件最核心的高频操作）、错题重生成、笔记重生成。用户在插件目录里手写的、或旧版本留下的多行备注，会在下一次复习时被静默截断。

建议：改用 Obsidian 的 `app.fileManager.processFrontMatter()`（YAML 感知），或检测到非扁平 YAML 时跳过重写、只做必要字段的追加。

### 6.5 🟡 其他新增小项

- `views/sidebar/review.ts:113-118`：`renderReviewRow` 只被 `dueItems`（已过滤）调用，`isOverdue` 恒为 `true`，`"{d}天后"` 分支是**死代码**。
- `utils/exporter.ts:290-293`：PDF 导出用 `loadURL("data:text/html;charset=utf-8," + encodeURIComponent(html))`。Chromium 对 data URL 长度有约 2MB 限制，**长试卷可能导出失败**；建议改走临时文件。
- `services/questionService.ts:366-379`：`parseAITagsFromResult` 只在**最后 5 行**内查找「知识点：」，模型末尾多输出几行（空行 / 补充说明）就会漏掉标签，导致知识点不入图谱。
- `utils/frontmatter.ts:49`：`buildFM` 的数组值不转义 —— tag 中含 `,` 或 `]` 会破坏 YAML（AI 生成标签时有一定概率命中）。
- `services/questionService.ts:361` vs `:299`：`validateGenerated` 只认加粗题号，`mergeExamChunks` 却兼容非加粗题号 —— 两者对「合法格式」的定义不一致（与 6.1 同源）。
- `utils/fs-utils.ts:1`：`safeName` 未处理 Windows 不允许的结尾点号/空格，以及控制字符。

### 6.6 合并后的修复优先级

| 优先级 | 问题 | 章节 | 改动量 |
|---|---|---|---|
| 1 | 导出解析器兼容加粗题号（一次修 4 个导出功能） | 6.1 | 小（3 处正则） |
| 2 | `openConfirm` / `openInput` 的 Esc 悬空 | 1.2 | 小 |
| 3 | 8 处绝对路径永久删除 → 统一走回收站 | 6.3 / 1.1 | 中 |
| 4 | 批量删除时索引重建只跑一次 | 6.2 | 小 |
| 5 | frontmatter 重写改为 YAML 感知 | 6.4 | 中 |
| 6 | `isCasualQuery` 长度阈值 | 2.2 | 小 |
| 7 | 原生设置页防抖写入 + `min`/`max` 钳制 | 2.3 / 2.4 | 小 |
| 8 | 实现 `getSettingDefinitions()`（1.13+ 设置搜索） | 3.2 | 中 |
| 9 | 统一 6 处原生 `confirm()` 到 `openConfirm` | 3.2 | 小 |
| 10 | `collectCandidates` 绝对路径 bug | 2.1 | 小 |
| 11 | `EASE_PRESETS` 文案 | 2.5 | 极小 |
| 12 | PDF 全局环境加互斥 | 3.3 | 中 |
| 13 | 提交 v2.3.0 改动并同步 README 测试数 | 3.8 | 极小 |

---

## 7. 第三轮：全部修复 + 回归验证

**本轮动作**：按 6.6 优先级表逐条修复 → 全量回归测试 → 重新审查。

### 7.1 指标对比（修前 → 修后）

| 检查 | 第二轮（修前） | 第三轮（修后） |
|------|------|------|
| `tsc --noEmit` | 0 错误 | **0 错误** |
| `eslint .` | 0 error / **12 warning** | **0 error / 0 warning** |
| `vitest run` | 239 / 239（19 文件） | **276 / 276（20 文件）** |
| `npm run build` | 通过 | 通过，`main.js` **2,886,554 B** |
| 构建护栏 | 通过 | **通过**（`createElement("script")` 0 次、`eval(` 0 次） |

### 7.2 修复明细

| # | 对应问题 | 修法 | 文件 |
|---|------|------|------|
| 1 | **6.1 加粗题号不兼容（🔴🔴）** | 题号 / 选项 / 子项 / 答案行 / 解析行 / 章节标题正则统一改为 `(?:\\*\\*)?…(?:\\*\\*)?`，同时兼容 `**1.**` 与 `**1**.` | `utils/text.ts`、`utils/exporter.ts`、`utils/layout.ts`、`utils/parse.ts`、`services/questionService.ts` |
| 2 | **1.2 弹窗 Esc 导致 Promise 永不 settle（🟠）** | `InputModal` / `ConfirmModal` 增加 `settled` 幂等标记，`onClose` 兜底以「取消值」settle | `views/ui/modals.ts` |
| 3 | **6.3 / 1.1 绝对路径永久删除（🔴，8 处）** | 新增 `TRASH_DIR_NAME = ".qg-trash"` 与 `trashFileAbs()`：优先 `shell.trashItem`，不可用时移入 `.qg-trash`（仍可恢复）；`.qg-trash` 纳入所有遍历排除规则；**`deleteFileAbs` 整体删除** | `utils/fs-utils.ts`、`utils/electron.ts`、`electron.d.ts`、`services/knowledgeService.ts`、`services/vaultDataService.ts`、`views/sidebar/{knowledge,notes,questions,wrong}.ts`、`views/sidebarView.ts` |
| 4 | **6.2 批量删除触发 N 次索引重建（🔴）** | `deleteWrongNote(filePath, skipRebuild = false)`，批量循环传 `true`，循环结束后只重建一次 | `services/vaultDataService.ts`、`views/sidebar/wrong.ts` |
| 5 | **6.4 frontmatter 被全量重写破坏（🟠）** | 新增 `patchFrontmatter(content, updates)`：只重写指定 key 的行，并吃掉其后的块标量 / 列表续行；复习评分、错题重生成、标签写入三条路径全部切换，不再走 `parseFM → buildFM → 整体替换`。同时加固 `parseFM`（块标量、`- item` 列表、跳过缩进行、数值 key 转型）与 `serializeEntry`（多行 → `\|` 块标量、数组展平） | `utils/frontmatter.ts`、`views/sidebarView.ts`、`views/sidebar/tagger.ts` |
| 6 | **2.2 短问题被误判为闲聊（🟠）** | 去掉 `length <= 4` 阈值，改为 `CASUAL_RE` + 纯语气词 `CASUAL_SHORT_RE` | `services/chatService.ts` |
| 7 | **2.3 / 2.4 逐键写盘 + 不钳制（🟠）** | 400ms 防抖 `scheduleSave()`，`hide()` 时 flush；新增 `clampSettingValue()` 对 `number` / `easePreset` 做 min/max 钳制，失焦回填钳制值 | `views/settingTab.ts`、`views/settingsSchema.ts` |
| 8 | **3.2 缺少声明式设置 API（🟡）** | 实现 `getSettingDefinitions()`（分组 + toggle/dropdown/number/text/slider + render 型备注与预设按钮），保留 `display()` 作为 < 1.13 降级；override `getControlValue`/`setControlValue`，避免基类实现丢掉 `history` | `views/settingTab.ts` |
| 9 | **3.2 原生 `confirm()` / 类型伪装（🟡）** | 全部确认弹窗统一走 `openConfirm`（现 0 处原生 `confirm`）；新增 `FileMeta` 抽象消除 4 处 `as unknown as TFile` | `types.ts`、`utils/filetree.ts`、`views/sidebarView.ts`、`views/sidebar/{notes,questions,noteGen,wrong,knowledge}.ts`、`views/notePickerModal.ts`、`services/knowledgeService.ts` |
| 10 | **2.1 插件范围检索恒为空（🟠）** | 新增 `normalizePluginDirs(dirs, vaultBasePath)`：绝对路径 → vault 相对路径，vault 外目录丢弃，Windows 大小写不敏感 | `services/chatService.ts`、`views/chatPanel.ts` |
| 11 | **2.5 预设文案与含义相反（🟠）** | `慢速(2.7)/快速(2.3)` → `少复习(2.7)/标准(2.5)/多复习(2.3)`，hint 加数值前缀 | `constants.ts`、`i18n/zh.ts`、`i18n/en.ts`、`README.md` |
| 12 | **3.3 PDF 全局环境竞态（🟡）** | `withPdfEnvironment` 加串行链（`pdfEnvChain` / `withPdfEnvironmentExclusive`），并发导出不再互相覆盖全局变量；`globalThis` → `window` | `services/documentService.ts` |
| 13 | **3.8 README 数字过期（🟡）** | 测试数 227 → 276，预设名中英文同步 | `README.md` |
| 14 | **6.5 杂项（🟡）** | PDF 导出 data URL → 临时文件（现已 0 处 `data:text/html`）；`parseAITagsFromResult` 扫描窗口 5 → 12 行、去尾部 `**`、支持 `,，、` 分隔；`buildFM` 数组值转义；`safeName` 处理结尾点 / 空格 / 控制字符；`review.ts` 死代码清理 | `utils/exporter.ts`、`services/questionService.ts`、`utils/frontmatter.ts`、`utils/text.ts`、`views/sidebar/review.ts` |
| 15 | **2.6 `MarkdownRenderer` 复用同一 Component（🟠，内存泄漏）** | `ChatPanel` 改为每条 AI 气泡一个 `MarkdownRenderChild`，用 `component.addChild(child)` 挂到宿主组件，`renderMessages()` / `destroy()` 时统一 `unload()` | `views/chatPanel.ts` |

**注**：`utils/exporter.ts:333` 仍保留 1 处 `fs.unlinkSync`，清理的是插件自己刚生成的**临时 PDF 文件**（非用户文件），不应进回收站——这是有意保留。

### 7.3 本轮新发现的问题

1. **`stripMdBold` 正则分组顺序错误** —— 由本轮新写的回归测试直接抓到。
   原式 `/^\*\*(\d+)\*\*([.、])/` 只能匹配 `**1**.` 这类写法；对插件 prompt 与 `validateGenerated` 实际要求的 `**1.**`，会输出成 `1.** 题干`。
   已修正为 `/^\*\*(\d+)([.、])\*\*/`（`**([A-D])` 同理）。
   → 这是**第二轮漏掉的第 2 个导出 bug**，与 6.1 同因不同位；现已修复且有测试覆盖。

2. **构建产物中 CJK 被转义（非缺陷，仅记录以免误判）** —— esbuild 默认 `charset: ascii`，产物内所有中文字面量都是 `\uXXXX` 形式。因此 `grep 少复习 main.js` 命中 0 属**假阴性**；正确做法是按转义序列检索（已验证 `少复习` / `多复习` / 已复制 等均以转义形式在包内）。

### 7.4 未做 / 建议后续

| 项 | 说明 | 建议 |
|---|---|---|
| **3.1 构建后正则手术** | `esbuild.config.mjs` 用正则改写压缩后的 `main.js`（剥离 `createElement("script")`、中和 `eval`）。依赖压缩器输出形态，esbuild 升级后可能**静默失效** | 长期改用 esbuild 插件 / loader；短期至少在 `tests/buildGuard.test.ts` 里把两条护栏断言写死（目前已有） |
| `minAppVersion` | 仍留在 1.12.x（声明式设置 API 需 1.13+，但 `display()` 降级完整可用，1.12 用户不受影响） | 若想启用设置搜索框，再升到 1.13.0 |
| 版本库状态 | 工作区仍有 40+ 文件未提交（含本轮全部改动与新建文件） | 建议一次性提交，附本报告 |
| 真机验证 | 本轮全部为静态检查 + 单元测试（vitest 用的是 `electron` / `obsidian` mock） | Electron 回收站、PDF 打印、1.13 设置页 UI 需在 Obsidian 内手测 |

### 7.5 建议手测清单

1. 复习评分一次 → 检查题目文件里手写的多行 / 嵌套 frontmatter 字段是否完好。
2. 同一份试卷导出 4 种版本（Word / PDF / 无答案 / 仅答案），确认题干、选项、答案、解析不串行。
3. 删除一道错题 → 到系统回收站（或 vault 下 `.qg-trash`）确认可找回。
4. 设置页切换「少复习 / 标准 / 多复习」，确认间隔变化方向与文案一致。
5. 打开设置页 → 顶部搜索框能搜到本插件设置项（Obsidian 1.13+）。
6. 对话面板连续多轮问答 → 观察长时间使用内存是否稳定（对应 2.6）。

### 7.6 总体结论

本轮把第二轮列出的 **13 项全部修完**，额外修掉 **1 项内存泄漏（2.6）**，并在修复过程中又发现并修掉 **1 个真实导出 bug**（7.3-1）。

指标由「0 error / 12 warning，239 测试」提升到「**0 error / 0 warning，276 测试**」，新增 `tests/regressions.test.ts`（37 条）作为防回归网。

**代码层面：通过，可发布。** 唯一保留意见是 7.4 的「构建后正则手术」依赖压缩产物形态，以及本轮缺少真机验证 —— 建议按 7.5 手测一遍后再提交发版。


---

## 8. 第四轮：边界压测复审（2026-09-20）

**触发**：用户追问「还有没有问题」。

这一轮换方法：**不再以通读代码为主，而是对第三轮新写/改写的代码做对抗性边界压测**
（临时写一次性测试直接调真函数，跑完把有效用例固化进 `tests/regressions.test.ts`）。

结果抓到 **7 个真问题**，其中 **4 个是靠压测（而不是读代码）发现的**，且有 **2 个是第三轮自己引入的**。
8.2 记录已直接修掉的 6 项，8.3 记录需要作者决策语义的那 1 项（决策后已落地）。

### 8.1 指标

| 检查 | 第三轮 | 第四轮 |
|------|------|------|
| `tsc --noEmit` | 0 错误 | **0 错误** |
| `eslint .` | 0 / 0 | **0 / 0** |
| `vitest run` | 276 / 276 | **300 / 300**（`regressions.test.ts` 37 → 61 条） |
| `npm run build` | 通过，2,886,554 B | 通过，**2,887,060 B** |
| 构建护栏 | 0 / 0 | **0 / 0** |

### 8.2 本轮直接修掉的 6 项

| # | 级别 | 问题 | 影响 | 修法 |
|---|------|------|------|------|
| N1 | 🔴 | **`patchFrontmatter` 非幂等：每次调用往 frontmatter 里多插一个空行** | **第三轮引入。** 复习评分是最高频操作，每次都往用户文件里塞一行空行且永不收敛（评 30 次 = 30 行空行）。根因：`fmText` 本身以换行开头，`split` 出的首个空元素被当作一行原样写回 | 拆行前先丢弃开头的空行 |
| N2 | 🟠 | **`patchFrontmatter` 遇到重复 key 会写出两行** | `interval: 1` + `interval: 2` → 写出两行 `interval: 6`（非法 YAML） | 同名 key 只保留第一处，其余丢弃 |
| N3 | 🟠 | **带 BOM 的文件被判为「没有 frontmatter」，于是插入第二个 frontmatter 块** | 原 frontmatter 沦为正文、用户元数据失效；`parseFM` 也读不到任何键 | `parseFM` / `patchFrontmatter` 统一先跳过 BOM，输出时再补回 |
| N4 | 🟠 | **`stripMdBold` 只认 `**1.**`，`**1**.` 会残留星号** | **第三轮引入。** 为了修 `**1.**` 换掉了原本匹配 `**1**.` 的那条正则，等于**修一种写法、坏另一种写法**；Word/PDF 里出现 `1**. 题干` | 两种星号位置都显式匹配；兜底从「盲切末尾两个星号」改为「只吃掉配对的那一组」 |
| N5 | 🟠 | **SM-2 的 EF 无上限、间隔无上限 → `nextReview` 被写成 `NaN-NaN-NaN`** | 实测：同一道错题连点「简单」约 **20 次**，`interval` 溢出成 `Invalid Date`，该条目从此**永远不会到期**；第 10 次时间隔已达 43,734 天（120 年后）。关键放大因素：错题卡片的质量按钮**不检查是否到期**，所以 20 连点完全可达 | EF 夹进 `[1.3, 3.0]`（与全项目使用的 `clampEase` / `EASE_MAX` 对齐）；新增 `MAX_INTERVAL_DAYS = 3650` 硬上限；乘 EF 时也先 `clampEase` |
| N6 | 🟡 | **`normalizePluginDirs` 不折叠重复斜杠** | `D://Vault//题目` 会算出 `/题目`，永远匹配不到任何 `f.path`（手输目录时容易多打一个斜杠） | 归一化时折叠 `//`，base 与目录两侧一致处理 |

### 8.3 N7：`isDueForReview` 空排期语义 —— 作者决策「采纳 A」，已修复

**问题**：`isDueForReview` 对 `nextReview` 为空返回 `false`。

`vaultDataService` 从磁盘读取时写的是 `nextReview: (meta.nextReview as string) || ""`。任何**没有 `nextReview` 字段**的错题 / 题目 / 笔记文件，既不会出现在「今日待复习」，也不计入启动提示「你有 N 道错题待复习」——**永久不可复习**。唯一出路是在错题本卡片里手动点评分，而卡片上显示的是「下次复习: 未设置」。

会缺这个字段的文件：手工创建的，以及早期插件版本创建的（插件自身写入的条目一定带 `nextReview`）。

**两种语义**：
- **A（符合 SRS 惯例，推荐）**：空 = 新条目 = **立即到期** → 这些文件重新进入复习队列。
- **B（原行为）**：空 = 不排期 → 那些文件永远不会被提醒。

**决策：采纳 A。** 实现要点：

| 项 | 处理 |
|---|---|
| 空值 / 缺失 / `undefined` | 判为**立即到期**（新条目） |
| 非法值（旧版 SM-2 溢出写出的 `NaN-NaN-NaN` 等非 `YYYY-MM-DD`） | 也判为**到期**，用户评一次分即写回合法排期，**文件自愈** |
| 合法未来日期 | 仍判为不到期（行为不变） |
| 插件自身写入的条目 | 一定带合法 `nextReview`，不受影响 |

**连带修复（列表角标）**：原先 `review.ts` / `wrong.ts` 的角标用 `if (note.nextReview)` 守卫，于是「未排期但已到期」的条目**既不显示「已到期」也不显示剩余天数**（一片空白）。已改为以 `isDueForReview` 为主判据：到期 → 「已到期」，仅排在未来 → 「{d}天后」。这同时消掉了第二轮 6.5 记录的那段死代码（`review.ts` 的行只来自已过滤的 `dueItems`，`{d}天后` 分支原本不可达）。

**已回归测试**（`tests/regressions.test.ts`）：空值 / 缺失 / `undefined` → true；`not-a-date`、`NaN-NaN-NaN`、`2026-9-1`、`20260901` → true；未来日期 → false；今天及更早 → true。

### 8.4 仍然遗留（工程债，非缺陷）

- **`vaultDataService` 有 6 处几乎完全相同的 `notes.push({...})`**（每个都是约 60 个字符挤在一行的字面量，把 meta 拼成 `WrongAnswerNote`）。任何字段改动都要改 6 遍，极易漏改——建议抽成 `toNote(meta, filePath, basename)` 工厂函数。
- `esbuild.config.mjs` 的构建后正则手术（第三轮 7.4 已记录）。
- `README` 测试数已再次同步（276 → **300**）。

### 8.5 结论

第四轮推翻了「改完即完」的假设：**第三轮自己引入了一个会持续污染用户文件的 bug（N1），还把一个导出 bug 从一种写法搬到了另一种写法（N4）。**
这说明对「刚修过的代码」只做静态复审是不够的——**必须做边界压测**。本轮 6 个修复里 4 个是压测发现的，1 个（N6）甚至是被 heredoc 意外产生的测试输入暴露出来的。

当前状态：`tsc` 0 错误、`eslint` 0/0、`vitest` **300/300**、`npm run build` 通过且护栏 0/0。
**代码层面可发布**；8.3 的语义决策已落地（采纳 A）。

---

## 9. 第五轮：清理遗留工程债（2026-09-20）

**触发**：用户「你帮我修复」。处理第四轮 8.4 列出的两项遗留。

### 9.1 指标

| 检查 | 第四轮 | 第五轮 |
|------|------|------|
| `tsc --noEmit` | 0 错误 | **0 错误** |
| `eslint .` | 0 / 0 | **0 / 0** |
| `vitest run` | 300 / 300 | **307 / 307** |
| `npm run build` | 通过，2,887,081 B | 通过，**2,884,060 B**（−3,021 B） |
| 构建护栏 | 断言在测试里 | **构建脚本内联断言 + 失败即还原产物** |

### 9.2 `vaultDataService` 的 6 处重复映射 → `toNote()`

**问题**：`loadAllWrongNotes` / `loadAllQuestionFilesForReview` / `loadAllVaultNotesForReview` 三个方法各自有 abs / vault 两个分支，共 **6 处**几乎完全相同的 `notes.push({...})`——每处都是**一整行 60+ 字符**的字面量（在编辑器里根本没法读，也没有 diff 可读性），任何字段改动都要改 6 遍。

**差异梳理**（只有两处真正不同，其余完全一致）：

| 扫描 | `wrongCount` 缺失时 | `source` 缺失时 |
|---|---|---|
| 错题本 | **1** | 留空 |
| 题库 | 0 | 留空 |
| 笔记视图 | 0 | **回退为文件名** |

**修法**：抽出纯函数 `toNote(meta, body, filePath, baseName, defaults)` + 三个具名默认值对象
（`WRONG_NOTE_DEFAULTS` / `QUESTION_NOTE_DEFAULTS` / `NOTE_VIEW_DEFAULTS`），6 处调用点各自传自己的默认值。
`defaults` 参数**必填**（不给默认值），漏传会直接编译报错，而不是悄悄用错默认值。

**行为等价性**：新增 7 条测试逐条锁定重构前的默认值语义（完整字段保留、空 frontmatter 兜底、
两种 `wrongCount` 默认值、`source` 回退差异、`repetitions = min(correctCount, 3)`、`lapses` 兜底、
类型不符退回默认）。**主产物因此小了 3 KB**，也侧面印证那 6 段确实是被复制粘贴的代码。

### 9.3 构建护栏：从「静默产出坏包」变成「报错并还原」

**问题**：`esbuild.config.mjs` 的 4 条构建后正则依赖压缩产物的具体形态。esbuild 或依赖升级后它们可能**静默失效**——构建照常成功、退出码 0，但 `main.js` 里带着会被 Obsidian 插件审核拒收的代码（动态 `<script>` 注入 / `no-eval`）。本地 `eslint` 也兜不住，因为 `main.js` 在 `globalIgnores` 里。

**先确认这些正则确实在干活**（打一个不打补丁的原始产物并统计）：

| 护栏目标 | 原始产物命中 | 补丁后（main.js） |
|---|---|---|
| `createElement("script")` | **8** | 0 |
| `"%eval%": eval` | **1** | 0 |
| `eval(` 调用 | 0 | 0 |

**修法**：在写盘前对终态做断言，任一不满足即 `process.exit(1)`，并打印**命中了什么、该去改哪条正则**。
另外因为 esbuild 在 `rebuild()` 时就已经把未打补丁的 bundle 写进了 `main.js`，所以补丁前先备份一份，
护栏失败时**把 `main.js` 还原成上一次的产物**（无旧产物则删除），确保磁盘上不会留下坏包。

**反向测试**（护栏本身也必须被测）：把第 4 条正则临时改成永不匹配，模拟「依赖升级后失效」：

```
✖ main.js 未通过构建护栏检查，构建中止：
  - "%eval%": eval：命中 1 次。docx 里 es-abstract 的 %eval% 表项没被中和 → 检查上面 #4 这条正则
  已将 main.js 还原为上一次的产物。
  若不修，Obsidian 插件审核会因动态脚本注入 / no-eval 规则直接拒收。
```

- 构建退出码 **1** ✅
- `main.js` 与构建前**逐字节一致**（`Buffer.equals` 为 true）✅
- 无 `main.js.guardbak` 残留 ✅
- 坏内容从未到达 `main.js` ✅

> 说明：完整替换成 esbuild 插件 / loader（彻底不依赖正则）工程量与回归风险都大，**未做**；
> 本轮做的是「让失效必然可见」，属于收益/风险比更高的那一半。

### 9.4 顺手修掉的仓库卫生问题

- `.gitignore` 里写的是 `.workbuddy/`，而实际目录是 `.workbuddy-ai/` → 工作区记忆/日志一直处于
  **未忽略**状态（`git status` 里一直有 `?? .workbuddy-ai/`）。已补上 `.workbuddy-ai/` 与 `main.js.guardbak`。
- `README` 测试数 300 → **307**。

### 9.5 结论

第四轮 8.4 列出的工程债已清零（`toNote` 重构 + 构建护栏）。当前：`tsc` 0 错误、`eslint` 0/0、
`vitest` **307/307**、`npm run build` 通过且护栏内联断言生效。

**代码层面可发布。** 剩下的都不是代码问题：仓库有 60+ 变更待提交、以及 7.5 的真机手测清单（Electron 回收站 / PDF 打印 / 1.13 设置页 UI）还没在 Obsidian 里实际跑过。
